def cubed(number)
    return number * number * number, 80# the return statement can return multiple values
    #effectively returning an array; elements can be accessed the same way
end

puts cubed(4) 